
  # MediResponse Healthcare Web App

  This is a code bundle for MediResponse Healthcare Web App. The original project is available at 
  https://skill-widen-88111346.figma.site
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
